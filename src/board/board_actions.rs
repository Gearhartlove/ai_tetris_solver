use crate::board::Board;
//use crate::board::block::block_type;

fn place_block(board: &Board, config: Vec<Vec<i8>>) {
    //
}

// fn rotate_block(board: &Board, block: block_type) -> Vec<Vec<usize>> {
//
// }